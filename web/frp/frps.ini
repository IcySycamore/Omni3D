# ============================================
# frps.ini —— 部署在公网服务器上
# 用 frp 提供 TCP/UDP 隧道，把内网的服务穿透到公网
# ============================================
[common]
# frp 控制端口
bind_port = 7000

# 可选：frp 内置的 HTTP 虚拟主机端口（多个 http 服务映射到同一 80/8080）
# vhost_http_port = 8080

# 可选：frp 面板（Web Dashboard）
# dashboard_port = 7500
# dashboard_user = admin
# dashboard_pwd = <请输入密码>

# token 认证
# auth.token = Umse30lxiv8nh
