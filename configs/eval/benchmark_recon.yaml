name: recon_mock
task: recon
output_dir: outputs/benchmark/recon_mock

backend:
  type: mock
  kwargs:
    num_points: 1000
    seed: 42

data:
  scene_dirs:
    - data/recon/scene_001
    - data/recon/scene_002
    - data/recon/scene_003

evaluator_kwargs:
  dist_th: 0.05
